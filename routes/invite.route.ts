import express from 'express';
// import { protectedRoute } from '../configs/protected-routes';
// import { generateInviteLink, verifyInviteLink } from '../controllers/invite.controller';

const router = express.Router();

// router.post('/create',protectedRoute,generateInviteLink);
// router.get('/:token',protectedRoute,verifyInviteLink);

export default router;